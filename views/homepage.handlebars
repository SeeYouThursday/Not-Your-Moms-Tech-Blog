<div class='dashboard'>
  <h2>Posts</h2>
  <div class='posts'>
    {{! render posts if user has any posts }}
    {{#if posts}}
    {{>post_content}}
    {{else}}
    <h3>No Posts Yet!</h3>
    {{!-- TODO make a link to the create post page if logged in, if not logged in, redirect to login page --}}
    {{/if}}
  </div>
</div>